import React from 'react';
import Valores from './components/valores/valores';

export default () => {
  useEffect(() => {
    effect;
    return () => {
      cleanup;
    };
  }, [input]);
  return <Valores />;
};
